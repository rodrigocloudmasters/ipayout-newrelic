# Credentials
.env

# Terraform
.terraform/
*.tfstate.backup
*.tfvars
crash.log

# Paste-ready install notes: these embed the ingest license key in plain text,
# so they stay out of the repo. Regenerate them from scripts/ if lost.
instalacion/
